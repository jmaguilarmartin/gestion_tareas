{
  "name": "calendario-actividades-mvp",
  "version": "1.0.0",
  "description": "Sistema de gestion de actividades con calendario y notificaciones",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Building...'",
    "deploy": "netlify deploy --prod",
    "get-token": "node scripts/get-refresh-token.js",
    "sync-env": "bash scripts/sync-env-netlify.sh",
    "sync-env:win": "powershell -ExecutionPolicy Bypass -File scripts/sync-env-netlify.ps1"
  },
  "dependencies": {
    "googleapis": "^128.0.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
